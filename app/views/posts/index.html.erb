<p id="notice"><%= notice %></p>
<body>
  <div class="column is-one-third is-offset-one-third">
    <div class="heading center-text border">
      <h1> Posts</h1>
    </div>
  </div>
  <div class="column is-half is-offset-one-quarter">
    <table>
          <!-- <thead>
            <tr>
              <th>Title</th>
              <th>Content</th>
              <th>User</th>
              <th colspan="3"></th>
            </tr>
          </thead> -->

          <tbody>
            <% @posts.each do |post| %>
              <tr class="box">
                <td><%= post.title %></td>
                <td><%= post.content %></td>
                <td><%= post.user.email %></td>
                <td><%= link_to 'Show',  topic_post_path(post.topic, post) %></td>
                
              </tr>
            <% end %>
          </tbody>
        </table>
        <br>
         <%= link_to 'New Post', new_topic_post_path %>
        </br>
      </div>
    </div>
  </div>
</body>
